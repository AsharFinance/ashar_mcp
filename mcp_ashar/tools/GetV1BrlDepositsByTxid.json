{
  "name": "GetV1BrlDepositsByTxid",
  "description": "Retrieve BRL Deposit by Transaction ID",
  "arguments": {
    "type": "object",
    "properties": {
      "api_key": {
        "type": "string",
        "minLength": 32,
        "description": "Ashar API key (Bearer token)"
      },
      "txid": {
        "type": "string",
        "minLength": 8,
        "maxLength": 128,
        "description": "Transaction ID of the PIX deposit (returned when the deposit is created)"
      }
    },
    "required": ["api_key", "txid"]
  }
}
